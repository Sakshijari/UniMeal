# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm install
      - name: Create .env.local for build
        env:
          NEXT_PUBLIC_FIREBASE_API_KEY: ${{ secrets.NEXT_PUBLIC_FIREBASE_API_KEY }}
          NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: ${{ secrets.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN }}
          NEXT_PUBLIC_FIREBASE_PROJECT_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_PROJECT_ID }}
          NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: ${{ secrets.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET }}
          NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID }}
          NEXT_PUBLIC_FIREBASE_APP_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_APP_ID }}
        run: |
          if [ -z "$NEXT_PUBLIC_FIREBASE_API_KEY" ] || [ -z "$NEXT_PUBLIC_FIREBASE_APP_ID" ]; then
            echo "::error::Missing Firebase secrets. In repo Settings → Secrets and variables → Actions, add the 6 NEXT_PUBLIC_FIREBASE_* secrets (values from .env.local)."
            exit 1
          fi
          printf 'NEXT_PUBLIC_FIREBASE_API_KEY=%s\n' "$NEXT_PUBLIC_FIREBASE_API_KEY" >> .env.local
          printf 'NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=%s\n' "$NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN" >> .env.local
          printf 'NEXT_PUBLIC_FIREBASE_PROJECT_ID=%s\n' "$NEXT_PUBLIC_FIREBASE_PROJECT_ID" >> .env.local
          printf 'NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=%s\n' "$NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET" >> .env.local
          printf 'NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=%s\n' "$NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID" >> .env.local
          printf 'NEXT_PUBLIC_FIREBASE_APP_ID=%s\n' "$NEXT_PUBLIC_FIREBASE_APP_ID" >> .env.local
      - run: npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_UNIMEAL_A59B8 }}
          projectId: unimeal-a59b8
